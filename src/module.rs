// Nu Module System
// 模块路径解析和管理

pub struct ModuleResolver {
    // TODO: 实现ModuleResolver
}

impl ModuleResolver {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for ModuleResolver {
    fn default() -> Self {
        Self::new()
    }
}
